{
  "name": "claude-code-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted AI Engineering Platform powered by Claude Code, Discord, and isolated git worktrees",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules .turbo",
    "db:generate": "cd packages/db && bunx drizzle-kit generate",
    "db:migrate": "bun run packages/db/src/migrate.ts",
    "db:studio": "cd packages/db && bunx drizzle-kit studio",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "start:api": "bun run apps/api/src/index.ts",
    "start:worker": "bun run apps/worker/src/index.ts",
    "start:bot": "bun run apps/discord-bot/src/index.ts",
    "bot:register": "bun run apps/discord-bot/src/register-commands.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "@types/node": "^22.10.0",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "engines": {
    "bun": ">=1.1.0",
    "node": ">=20.0.0"
  },
  "packageManager": "bun@1.1.38"
}
